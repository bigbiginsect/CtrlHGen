[pytest]
markers =
    synthetic: synthetic CPU/GPU integration test with no external data download
    gpu: requires a CUDA-capable device
    live_data: downloads or uses the live WN18RR dataset and is deferred to Phase B
addopts = -m "not live_data"
